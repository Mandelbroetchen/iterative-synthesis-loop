# Design Documentation Generator Prompt

## Overview

You are a **design documentation generator** responsible for creating and updating comprehensive artifacts based on project specifications.

**Objective**: Generate or update files in `prompt-final/` according to explicit instructions in `prompt-raw`, using the project description as the single source of truth. When writing mathematical expressions, don't use special symbols that may cause problems. Use LaTeX for math expressions. 

---

## Part 1: Project Specification (Input)

Everything in `prompt-raw/` serves as your input data source.

### 1.1 Project Description
**File**: `prompt-raw/description.md`

This document defines the complete system scope:
- Purpose and goals
- Functional requirements  
- Use cases and workflows
- Technical architecture
- Key constraints and assumptions


<file src = "description.md">

# Freefall of an apple
A minimal program that takes parameters as input and prints the evolution of variables through time. 
</file>



---

## Part 2: Current Artifacts (Reference State)

These files exist in `prompt-final/` and represent the current state. Update them according to the instructions specified in `prompt-raw/`.

### 2.1 document.md
**Purpose**: Comprehensive system documentation including requirements, architecture, and design decisions.


<file src = "document.md">

# Freefall of an Apple - System Documentation

## 1. Purpose and Goals
This system simulates the freefall motion of an apple under constant gravitational acceleration. The program takes initial parameters as input and outputs the evolution of physical variables (position, velocity, acceleration) over time.

### Key Objectives:
- Model one-dimensional freefall motion with constant acceleration
- Calculate and display position, velocity, and acceleration at discrete time intervals
- Provide a simple, educational demonstration of basic physics principles

## 2. Functional Requirements

### 2.1 Input Requirements
The system shall accept the following input parameters:
- Initial height (h₀): Starting position of the apple (meters)
- Initial velocity (v₀): Starting velocity of the apple (meters/second)
- Time step (Δt): Interval between calculations (seconds)
- Total duration (T): Total simulation time (seconds)
- Gravitational acceleration (g): Typically 9.81 m/s² (meters/second²)

### 2.2 Processing Requirements
The system shall:
1. Calculate position at each time step using:
   h(t) = h₀ + v₀ * t + 0.5 * g * t²
2. Calculate velocity at each time step using:
   v(t) = v₀ + g * t
3. Calculate acceleration (constant):
   a(t) = g
4. Iterate through time steps from t = 0 to t = T in increments of Δt

### 2.3 Output Requirements
The system shall output a table showing:
- Time (t)
- Position (h)
- Velocity (v)
- Acceleration (a)

## 3. Technical Architecture

### 3.1 System Components
1. **Input Handler**: Collects and validates user input
2. **Physics Engine**: Performs freefall calculations
3. **Output Formatter**: Formats results for display

### 3.2 Key Constraints
- Assumes constant gravitational acceleration (no air resistance)
- One-dimensional motion only
- No collision detection (apple falls indefinitely)

### 3.3 Assumptions
- Earth's gravity (g = 9.81 m/s²) unless specified otherwise
- Perfectly vertical freefall
- Time steps are uniform

## 4. Mathematical Model

The system implements the following equations of motion:

1. Position as a function of time:
   h(t) = h₀ + v₀ * t + (1/2) * g * t²

2. Velocity as a function of time:
   v(t) = v₀ + g * t

3. Acceleration (constant):
   a(t) = g

## 5. Example Use Case

**Scenario**: Simulate an apple falling from 10 meters with initial velocity of 0 m/s.

**Input**:
- h₀ = 10 m
- v₀ = 0 m/s
- Δt = 0.1 s
- T = 1.5 s
- g = 9.81 m/s²

**Expected Output**:
```
Time (s) | Position (m) | Velocity (m/s) | Acceleration (m/s²)
---------------------------------------------------------------
0.0      | 10.00        | 0.00           | 9.81
0.1      | 9.95         | 0.98           | 9.81
0.2      | 9.80         | 1.96           | 9.81
...      | ...          | ...            | ...
1.5      | 2.53         | 14.72          | 9.81
```
</file>


### 2.2 usecase-diagram.uml
**Purpose**: Visual representation of user interactions and system actors (UML Use Case Diagram).


<file src = "usecase-diagram.uml">

@startuml usecase-diagram

left to right direction

actor User

rectangle FreefallSimulation {
  usecase (Input Parameters) as UC1
  usecase (Run Simulation) as UC2
  usecase (View Results) as UC3
}

User --> UC1
User --> UC2
User --> UC3

UC1 .> UC2 : includes
UC2 .> UC3 : includes

@enduml

</file>


### 2.3 package-diagram.uml
**Purpose**: Module/package structure and dependencies (UML Package Diagram).


<file src = "package-diagram.uml">

@startuml package-diagram

package "Freefall Simulation" {
  package "Input" {
    [Parameter Collector]
  }

  package "Physics" {
    [Freefall Calculator]
  }

  package "Output" {
    [Result Formatter]
  }
}

[Parameter Collector] --> [Freefall Calculator]
[Freefall Calculator] --> [Result Formatter]

@enduml

</file>


### 2.4 class-diagram.uml
**Purpose**: Data model and relationships between system entities (UML Class Diagram).


<file src = "class-diagram.uml">

@startuml class-diagram

class FreefallSimulator {
  -h0: float
  -v0: float
  -dt: float
  -T: float
  -g: float
  +__init__(h0: float, v0: float, dt: float, T: float, g: float = 9.81)
  +calculate_position(t: float): float
  +calculate_velocity(t: float): float
  +run_simulation(): list
}

class SimulationResult {
  +time: list
  +position: list
  +velocity: list
  +acceleration: list
  +display(): str
}

FreefallSimulator --> SimulationResult : produces

@enduml

</file>


---

## Part 3: Processing Requirements

### Rules
1. **Only update files listed in `.overwrite.json`**
2. **Respect the specified mode for each file**
3. **Preserve existing structure** in completion and correction modes
4. **Maintain consistency** across all artifacts
5. **Ensure all content is complete and production-ready**

### Validation Checklist
- [ ] Each file contains complete, coherent content
- [ ] No placeholder text or incomplete sections remain
- [ ] UML diagrams use valid syntax
- [ ] Markdown is properly formatted
- [ ] Cross-references and terminology are consistent
- [ ] Generated code is syntactically correct

---

## Part 4: Output Format (STRICT)

**You must respond with exactly one file: `overwrite.py`**

Requirements:
- Pure Python code only (no explanations, comments, or additional text)
- Complete, production-ready content for each file
- UTF-8 encoding
- Creates directories if needed
- Successfully executes without errors

**Template Structure**:

```python
from pathlib import Path

files = {
    "prompt-final/document.md": """[Complete markdown content]""",
    "prompt-final/usecase-diagram.uml": """[Complete PlantUML diagram]""",
    "prompt-final/package-diagram.uml": """[Complete PlantUML diagram]""",
    "prompt-final/class-diagram.uml": """[Complete PlantUML diagram]"""
}

for path, content in files.items():
    Path(path).parent.mkdir(parents=True, exist_ok=True)
    Path(path).write_text(content, encoding="utf-8")

print("✓ All files updated successfully")
```

Do not include any additional output or explanation beyond the Python code. Start with a single line ` ```python ` and end with a single line ` ``` `. 