# Design Prompt for prompt-final Completion
The following promt is generated from the two folders `prompt-raw` and `prompt-final`. The purpose of this prompt is to overwrite files in `prompt-final`, based on the generated instructions generated from `prompt-raw`.  

## Instructions
This part of the prompt is generated from `prompt-raw`

### Description
`description.md` is a markdown file that describes the main prompt-raw.
```
<insert src="./prompt-raw/description.md"></insert>
```

### Overwrite
`overwrite.json` is a json file that specifies which files are to be overwritten (generation, completion or correction). 
```
<insert src="./prompt-raw/.overwrite.json"></insert>
```

## prompt-final
This part of the prompt describes current state of `prompt-final`

### `prompt-final/document.md`
This is a general documentation of the final project. 
```
<insert src="./prompt-final/document.md"></insert>
```
### `prompt-final/usecase-diagram.uml`
```
<insert src="./prompt-final/usecase-diagram.uml"></insert>
```

### `prompt-final/package-diagram.uml`
```
<insert src="./prompt-final/package-diagram.uml"></insert>
```

### `prompt-final/class-diagram.uml`
```
<insert src="./prompt-final/class-diagram.uml"></insert>
```

## Output
Your response must strictly be a file "overwrite.py" that overwrites the files in `prompt-final` as specified in `prompt-raw/overwrite.json`. Do not include any explanations or additional text. Only provide the complete content of the overwritten files.