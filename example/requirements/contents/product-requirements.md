# Blackhole simulation
- read a config.json file as user input for physical parameters and simulation parameters
- read a objects.json file as user input for physical objects (celestial objects)
- write all data in `./out/`